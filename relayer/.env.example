# Cross-Chain Relayer - .env.example
# Copy this file to `.env` and fill in the required values.
# Notes:
# - Prefer ETHEREUM_PRIVATE_KEY over deprecated DEPLOYER_PRIVATE_KEY
# - NEAR relayer uses NEAR_RELAYER_* variables (account + key)
# - For Sepolia testnet, ETHEREUM_CHAIN_ID=11155111

# ===== NEAR Configuration =====
NEAR_NETWORK_ID=testnet
NEAR_NODE_URL=https://rpc.testnet.near.org
NEAR_RELAYER_ACCOUNT_ID=relayer.testnet
NEAR_RELAYER_PRIVATE_KEY=ed25519:YOUR_NEAR_SECRET_KEY
# NEAR escrow contract handling swap orders
NEAR_ESCROW_CONTRACT_ID=escrow.yourproject.testnet

# (Legacy override support via ConfigurationService; prefer NEAR_RELAYER_* above)
# NEAR_ACCOUNT_ID=
# NEAR_PRIVATE_KEY=

# ===== Ethereum Configuration =====
ETHEREUM_RPC_URL=https://sepolia.infura.io/v3/YOUR_KEY
ETHEREUM_CHAIN_ID=11155111
ETHEREUM_PRIVATE_KEY=0xYOUR_ETH_PRIVATE_KEY
# Deprecated but temporarily supported. Prefer ETHEREUM_PRIVATE_KEY
# DEPLOYER_PRIVATE_KEY=

# Deployed contract addresses
ETHEREUM_ESCROW_FACTORY_ADDRESS=0xFactoryAddressHere
ETHEREUM_BRIDGE_ADDRESS=0xBridgeAddressHere
# Optional: defaults to ETHEREUM_ESCROW_FACTORY_ADDRESS if not set
RESOLVER_ADDRESS=0xResolverAddressHere

# (Legacy config keys still parsed by ConfigurationService; prefer the above)
# ETHEREUM_ESCROW_CONTRACT=
# ETHEREUM_BRIDGE_CONTRACT=

# ===== Relayer Runtime =====
# Poll interval for both relayers (ms). Recommended to use this var.
RELAYER_POLL_INTERVAL=5000
# Legacy name used in config loader (won't affect src/index.ts runtime polling)
# POLLING_INTERVAL=5000

# Storage directory for persistent data
STORAGE_DIR=./storage

# Logging
LOG_LEVEL=info
# Enable file logs (rotating files) unless set to 'false'
ENABLE_FILE_LOGS=true

# Process control
# Auto start relayer on process launch (set to 'false' to only import without starting)
RELAYER_AUTO_START=true
NODE_ENV=development
PORT=3000

# ===== Price Oracle (optional overrides) =====
# Override exchange rates used by DynamicAuctionService/PriceOracleService
# Expressed as: 1 <FROM> = <rate> <TO>
# Example: 1 NEAR ≈ 0.001006 ETH
EXCHANGE_RATE_NEAR_TO_ETH=0.001006
# Example reverse rate if needed
# EXCHANGE_RATE_ETH_TO_NEAR=1000

# ===== Dynamic Auction (optional tuning) =====
# Total auction duration in seconds
AUCTION_DURATION=180
# Initial rate bump in basis points (e.g., 60000 = +600%)
AUCTION_INITIAL_RATE_BUMP=60000
# Time points JSON (seconds + bump bps); must be valid JSON
# Example: at t=0: +600%, t=60: +300%, t=180: +0%
AUCTION_POINTS=[{"delay":0,"coefficient":60000},{"delay":60,"coefficient":30000},{"delay":180,"coefficient":0}]
# Estimated extra gas usage (units) and gas price (gwei) for safety deposits
AUCTION_GAS_BUMP_ESTIMATE=100000
AUCTION_GAS_PRICE_ESTIMATE=15
# Minimum partial fill percentage (0..1)
AUCTION_MIN_FILL_PERCENTAGE=0.1
# Maximum rate bump cap in basis points
AUCTION_MAX_RATE_BUMP=80000
